@using Microsoft.IdentityModel.Tokens
@model IEnumerable<Core.Publisher>

@{
    ViewData["Title"] = "Publishers";
}

<h1>Publishers</h1>

<div class="mt-3">
    <div class="flex-row mb-2">
        <button>
            <a class="text-decoration-none" asp-area="" asp-controller="Publisher" asp-action="Create">Create</a>
        </button>
        @if (!Model.IsNullOrEmpty())
        {
            <button>
                <a class="text-decoration-none" asp-area="" asp-controller="Publisher" asp-action="Update">Update</a>
            </button>
            <button>
                <a class="text-decoration-none" asp-area="" asp-controller="Publisher" asp-action="Delete">Delete</a>
            </button>
        }
    </div>
    @if (!Model.IsNullOrEmpty())
    {
        <table class="table table-striped">
            <thead class="thead-dark">
            <tr>
                <th>Publisher's ID</th>
                <th>Publisher's name</th>
            </tr>
            </thead>
            <tbody>
            @foreach (var developer in Model)
            {
                <tr>
                    <td>@developer.PublisherId</td>
                    <td>@developer.PublisherName</td>
                </tr>
            }
            </tbody>
        </table>
    }
    else
    {
        <p>No publishers were found!</p>
    }
</div>
<a asp-controller="AdminPanel" asp-action="Index" class="mt-2">Back to admin-panel</a>
