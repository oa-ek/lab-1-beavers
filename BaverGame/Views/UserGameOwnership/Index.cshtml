@using Microsoft.IdentityModel.Tokens
@model IEnumerable<Core.UserGameOwnership>

@{
    ViewData["Title"] = "UserGameOwnership";
}

<h1>UserGameOwnership</h1>

<div class="mt-3">
    <div class="flex-row mb-2">
        <button>
            <a class="text-decoration-none" asp-area="" asp-controller="UserGameOwnership" asp-action="Create">Create</a>
        </button>
        @if (!Model.IsNullOrEmpty())
        {
            <button>
                <a class="text-decoration-none" asp-area="" asp-controller="UserGameOwnership" asp-action="Update">Update</a>
            </button>
            <button>
                <a class="text-decoration-none" asp-area="" asp-controller="UserGameOwnership" asp-action="Delete">Delete</a>
            </button>
        }
    </div>
    @if (!Model.IsNullOrEmpty())
    {
        <table class="table table-striped">
            <thead class="thead-dark">
            <tr>
                <th>UserGameOwnership's ID</th>
                <th>User</th>
                <th>Game</th>
            </tr>
            </thead>
            <tbody>
            @foreach (var ownership in Model)
            {
                <tr>
                    <td>@ownership.OwnershipId</td>
                    <td>@ownership.User</td>
                    <td>@ownership.Game</td>
                </tr>
            }
            </tbody>
        </table>
    }
    else
    {
        <p>No gameOwnerShips were found!</p>
    }
</div>
<a asp-controller="AdminPanel" asp-action="Index" class="mt-2">Back to admin-panel</a>