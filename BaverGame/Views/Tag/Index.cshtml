@using Microsoft.IdentityModel.Tokens
@model IEnumerable<Core.Tag>

@{
    ViewData["Title"] = "Tags";
}

<div class="mx-4 h-auto my-4 w-100 text-center">
    <h1>Tags</h1>

    <div class="mt-3">
        <div class="flex-row mb-2">
            <button type="button" class="btn btn-dark m-2">
                <a class="text-decoration-none" asp-area="" asp-controller="Tag" asp-action="Create">Create</a>
            </button>
        </div>
        @if (!Model.IsNullOrEmpty())
        {
            <table class="table table-striped">
                <thead class="thead-dark">
                    <tr>
                        <th>Tag's name</th>
                        <th></th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var tag in Model)
                    {
                        <tr>
                            <td>@tag.TagName</td>
                            <td>
                                <button type="button" class="btn btn-dark">
                                    <a class="text-decoration-none" asp-area="" asp-controller="Tag" asp-route-id="@tag.TagId" asp-action="Update">Update</a>
                                </button>
                            </td>
                            <td>
                                <button type="button" class="btn btn-dark">
                                    <a class="text-decoration-none" asp-area="" asp-controller="Tag" asp-route-id="@tag.TagId" asp-action="Delete">Delete</a>
                                </button>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
        else
        {
            <p>No tags were found!</p>
        }
    </div>
    <a asp-controller="AdminPanel" asp-action="Index" class="mt-2">Back to admin-panel</a>
</div>

