@using Microsoft.IdentityModel.Tokens
@model IEnumerable<Core.GameTag>

@{
    ViewData["Title"] = "Game tags";
}

<h1>Game tags</h1>

<div class="mt-3">
    <div class="flex-row mb-2">
        <button>
            <a class="text-decoration-none" asp-area="" asp-controller="GameTag" asp-action="Create">Create</a>
        </button>
    </div>
    @if (!Model.IsNullOrEmpty())
    {
        <table class="table table-striped">
            <thead class="thead-dark">
            <tr>
                <th>Tag</th>
                <th>Game</th>
                <th></th>
                <th></th>
            </tr>
            </thead>
            <tbody>
            @foreach (var tag in Model)
            {
                <tr>
                    <td>@tag.Tag.TagName</td>
                    <td>@tag.Game.Name</td>
                    <td>
                        <button>
                            <a class="text-decoration-none" asp-area="" asp-controller="GameTag" asp-route-id="@tag.GameTagId" asp-action="Update">Update</a>
                        </button>
                    </td>
                    <td>
                        <button>
                            <a class="text-decoration-none" asp-area="" asp-controller="GameTag" asp-route-id="@tag.GameTagId" asp-action="Delete">Delete</a>
                        </button>
                    </td>
                </tr>
            }
            </tbody>
        </table>
    }
    else
    {
        <p>No game tags were found!</p>
    }
</div>
<a asp-controller="AdminPanel" asp-action="Index" class="mt-2">Back to admin-panel</a>
